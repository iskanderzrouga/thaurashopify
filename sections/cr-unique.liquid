{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top_m }}px;
    padding-bottom: {{ section.settings.padding_bottom_m }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div
  id="{{ section.settings.section_id }}"
  class="section-{{ section.id }}-padding px-5 relative"
  style="background-color: {{ section.settings.bgcolor }}"
>
  {% if section.settings.bg != blank %}
    {{
      section.settings.bg
      | image_url: width: 1920
      | image_tag: class: 'w-full absolute top-0 left-0 h-full object-cover', sizes: '1920px'
    }}
  {% endif %}

  <div
    class="mx-auto w-full max-w-[1420px] flex flex-col items-center justify-center gap-8 relative"
    style="max-width: {{ section.settings.max_width }}px;"
  >
    <div
      class="flex flex-col items-center justify-center gap-10 text-center mx-auto relative w-full"
    >
      <div
        class="relative px-5 py-6 md:p-10 w-full border border-[#AAAAAA] rounded-[16px]"
        style="background: linear-gradient(to bottom, #FFFFFF, #D0D0D0);"
      >
        <div class="relative flex flex-col w-full gap-[30px] md:gap-10">
          {% for block in section.blocks %}
            {% case block.type %}
              {% when 's1_contents' %}
                <div
                  id="{{ block.settings.id }}"
                  class="relative {% if block.settings.hide_mobile %} max-md:hidden {% endif %} {% if block.settings.hide_desktop %} md:hidden {% endif %} flex flex-col w-full gap-[30px] md:gap-10"
                >
                  {% if block.settings.heading != blank %}
                    <h2
                      class="text-[28px] md:text-[36px] font-bold !leading-tight text-[#00754A] text-center"
                      style="color: {{ block.settings.heading_color }}"
                    >
                      {{ block.settings.heading }}
                    </h2>
                  {% endif %}

                  {% if block.settings.description != blank %}
                    <div
                      class="text-[16px] md:text-[20px] font-normal !leading-tight text-[#00754A] text-center [&>ul]:list-disc [&>ul]:text-left [&>ul]:mx-auto [&>ul]:w-fit [&>ul>li]:w-fit [&>ul]:max-md:bg-white [&>ul]:max-md:rounded-[10px] [&>ul]:max-md:p-3 [&>ul]:!pl-7 [&>ul>li]:md:pt-3"
                      style="color: {{ block.settings.description_color }}"
                    >
                      {{ block.settings.description }}
                    </div>
                  {% endif %}

                  {% if block.settings.image != blank %}
                    <div class="flex flex-col items-center justify-center text-center gap-3">
                      {% if block.settings.img_label != blank %}
                        <div
                          class="text-[20px] md:text-[20px] font-normal !leading-tight text-[#00754A] text-center"
                          style="color: {{ block.settings.img_label_color }}"
                        >
                          {{ block.settings.img_label }}
                        </div>
                      {% endif %}
                      {{ block.settings.image | image_url: width: 1200 | image_tag: class: 'w-full' }}
                    </div>
                  {% endif %}

                  {% if block.settings.tab_parent %}
                    {% assign parent_id = block.settings.id %}

                    <!-- Tabs -->
                    <div class="w-full flex flex-col gap-3">
                      {% for block in section.blocks %}
                        {% case block.type %}
                          {% when 's1_tab' %}
                            {% if parent_id == block.settings.id %}
                              <div
                                data-id="{{ block.id }}"
                                data-p-id="{{ parent_id }}"
                                class="car-unique-s1-tab p-2 rounded-[100px] cursor-pointer text-[20px] md:text-[20px] font-normal !leading-tight text-[#fff] text-center"
                                style="color: {{ block.settings.img_label_color }}; background-color: {{ block.settings.img_label_bgcolor }}"
                              >
                                {{ block.settings.img_label }}
                              </div>
                            {% endif %}
                        {% endcase %}
                      {% endfor %}
                    </div>

                    <!-- Splide slider (mobile only) -->
                    <div class="w-full block md:hidden">
                      <div id="splide-{{ parent_id }}" class="splide">
                        <div class="splide__track">
                          <ul class="splide__list">
                            {% for block in section.blocks %}
                              {% case block.type %}
                                {% when 's1_tab' %}
                                  {% if parent_id == block.settings.id and block.settings.image != blank %}
                                    <li class="splide__slide" data-id="{{ block.id }}">
                                      {{
                                        block.settings.image
                                        | image_url: width: 1200
                                        | image_tag: class: 'w-full rounded-[20px]'
                                      }}
                                    </li>
                                  {% endif %}
                              {% endcase %}
                            {% endfor %}
                          </ul>
                        </div>
                      </div>
                    </div>

                    <!-- Desktop images (hidden on mobile) -->
                    <div class="w-full hidden md:block">
                      {% for block in section.blocks %}
                        {% case block.type %}
                          {% when 's1_tab' %}
                            {% if parent_id == block.settings.id and block.settings.image != blank %}
                              <div
                                data-id="{{ block.id }}"
                                data-p-id="{{ parent_id }}"
                                class="car-unique-s1-tab-img {% if block.settings.active %} active {% endif %} hidden [&.active]:block"
                              >
                                {{
                                  block.settings.image
                                  | image_url: width: 1200
                                  | image_tag: class: 'w-full rounded-[20px]'
                                }}
                              </div>
                            {% endif %}
                        {% endcase %}
                      {% endfor %}
                    </div>
                    <script>
                      document.addEventListener('DOMContentLoaded', function () {
                        const splide = new Splide('#splide-{{ parent_id }}', {
                          arrows: true,
                          pagination: false,
                          type: 'slide',
                          rewind: true,
                          perPage: 1,
                        }).mount();

                        // Tab click → move slider
                        document.querySelectorAll('.car-unique-s1-tab').forEach((tab) => {
                          tab.addEventListener('click', () => {
                            const id = tab.getAttribute('data-id');
                            const slideIndex = Array.from(
                              document.querySelectorAll('#splide-{{ parent_id }} .splide__slide')
                            ).findIndex((slide) => slide.getAttribute('data-id') === id);

                            if (slideIndex >= 0) {
                              splide.go(slideIndex);
                            }
                          });
                        });
                      });
                    </script>
                    <style>
                      .section-{{ section.id }}-padding #splide-{{ parent_id }} .splide__arrow--prev {
                        background: #00754A;
                        opacity: 1;
                        left: -10px;
                      }
                      .section-{{ section.id }}-padding #splide-{{ parent_id }} .splide__arrow--next {
                        background: #00754A;
                        opacity: 1;
                        right: -10px;
                      }
                      .section-{{ section.id }}-padding #splide-{{ parent_id }} .splide__arrow svg {
                        fill: white;
                        width: 12px;
                        height: 12px;
                      }
                    </style>
                  {% endif %}

                  {% if block.settings.card_parent %}
                    {% assign parent_id = block.settings.id %}
                    <div class="w-full grid grid-cols-3 gap-2 md:hidden">
                      {% for block in section.blocks %}
                        {% case block.type %}
                          {% when 's1_card' %}
                            {% if parent_id == block.settings.id %}
                              <div
                                data-id="{{ block.id }}"
                                data-p-id="{{ parent_id }}"
                                class="car-unique-s1-card p-2 {% if  block.settings.active %} active {% endif %} rounded-[100px] cursor-pointer text-[14px] font-bold !leading-tight text-[#000] text-center [&.active]:text-white border border-[#084C25] bg-white [&.active]:bg-[#084C25]"
                              >
                                {{ block.settings.tab }}
                              </div>
                            {% endif %}
                        {% endcase %}
                      {% endfor %}
                    </div>
                    <div class="w-full grid grid-cols-1 md:grid-cols-3 gap-[10px]">
                      {% for block in section.blocks %}
                        {% case block.type %}
                          {% when 's1_card' %}
                            {% if parent_id == block.settings.id %}
                              <div
                                data-id="{{ block.id }}"
                                data-p-id="{{ parent_id }}"
                                class="{% if  block.settings.active %} active {% endif %} hidden [&.active]:flex md:!flex car-unique-s1-card-one rounded-[12px] border border-[#1C5934] flex-col"
                              >
                                {% if block.settings.image != blank %}
                                  {{
                                    block.settings.image
                                    | image_url: width: 1200
                                    | image_tag: class: 'w-full rounded-t-[12px]'
                                  }}
                                {% endif %}
                                <div class="p-5 flex flex-col items-center text-center gap-1 bg-white rounded-b-[12px] flex-1">
                                  {% if block.settings.heading1 != blank %}
                                    <h2
                                      class="text-[20px] md:text-[20px] font-bold !leading-tight text-[#fff] text-center bg-[#084C25] px-1 py-[2px] w-fit"
                                    >
                                      {{ block.settings.heading1 }}
                                    </h2>
                                  {% endif %}
                                  {% if block.settings.heading2 != blank %}
                                    <h2
                                      class="text-[20px] md:text-[20px] font-bold !leading-tight text-[#fff] text-center bg-[#084C25] px-1 py-[2px] w-fit"
                                    >
                                      {{ block.settings.heading2 }}
                                    </h2>
                                  {% endif %}
                                  {% if block.settings.description != blank %}
                                    <div
                                      class="mt-3 text-[16px] md:text-[16px] font-normal !leading-tight text-[#252726] text-center [&>ul]:list-disc [&>ul]:text-left [&>ul]:mx-auto [&>ul]:w-fit [&>ul>li]:w-fit [&>ul]:max-md:bg-white [&>ul]:max-md:rounded-[10px] [&>ul]:max-md:p-3 [&>ul]:!pl-7"
                                    >
                                      {{ block.settings.description }}
                                    </div>
                                  {% endif %}
                                </div>
                              </div>
                            {% endif %}
                        {% endcase %}
                      {% endfor %}
                    </div>
                  {% endif %}
                </div>
            {% endcase %}
          {% endfor %}
        </div>
      </div>

      <div
        class="relative py-6 md:py-10 w-full border border-[#AAAAAA] rounded-[16px]"
        style="background: linear-gradient(to bottom, #FFFFFF, #D0D0D0);"
      >
        <div class="relative flex flex-col w-full gap-[30px] md:gap-10">
          {% for block in section.blocks %}
            {% case block.type %}
              {% when 's2_contents' %}
                <div class="relative {% if block.settings.hide_mobile %} max-md:hidden {% endif %} {% if block.settings.hide_desktop %} md:hidden {% endif %} flex flex-col w-full gap-[30px] md:gap-10">
                  {% if block.settings.heading != blank %}
                    <h2
                      class="text-[28px] md:text-[36px] font-bold !leading-tight text-[#00754A] text-center px-5 md:px-10"
                      style="color: {{ block.settings.heading_color }}"
                    >
                      {{ block.settings.heading }}
                    </h2>
                  {% endif %}

                  {% if block.settings.description != blank %}
                    <div
                      class="text-[16px] md:text-[20px] font-normal !leading-tight text-[#00754A] text-center [&>ul]:list-disc [&>ul]:text-left [&>ul]:mx-auto [&>ul]:w-fit [&>ul>li]:w-fit [&>ul]:max-md:bg-white [&>ul]:max-md:rounded-[10px] [&>ul]:max-md:p-3 [&>ul]:!pl-7 px-5 md:px-10 [&>ul>li]:md:pt-3"
                      style="color: {{ block.settings.description_color }}"
                    >
                      {{ block.settings.description }}
                    </div>
                  {% endif %}

                  {% if block.settings.image != blank %}
                    <div class="flex flex-col items-center justify-center text-center gap-3 px-5 md:px-10">
                      {% if block.settings.img_label != blank %}
                        <div
                          class="text-[20px] md:text-[20px] font-normal !leading-tight text-[#00754A] text-center"
                          style="color: {{ block.settings.img_label_color }}"
                        >
                          {{ block.settings.img_label }}
                        </div>
                      {% endif %}
                      {{ block.settings.image | image_url: width: 1200 | image_tag: class: 'w-full' }}
                    </div>
                  {% endif %}

                  {% if block.settings.review_parent %}
                    {% assign parent_id = block.settings.id %}
                    <div class="w-full flex flex-col gap-3 px-5 md:px-10">
                      {% for block in section.blocks %}
                        {% case block.type %}
                          {% when 's2_review' %}
                            {% if parent_id == block.settings.id %}
                              <div class=" bg-[#F1E0E036] border border-[#AAAAAA] rounded-[16px] w-full grid grid-cols-1 md:grid-cols-2 max-w-[800px] mx-auto">
                                {% if block.settings.image != blank %}
                                  {{
                                    block.settings.image
                                    | image_url: width: 500
                                    | image_tag:
                                      class: 'w-full h-full object-cover rounded-t-[16px] rounded-bl-[1px] md:rounded-bl-[16px] md:rounded-tr-[1px]',
                                      sizes: '500px'
                                  }}
                                {% endif %}

                                <div class="h-full flex flex-col justify-center gap-4 items-center md:items-start p-5 md:p-10">
                                  {% if block.settings.stars != blank %}
                                    {{
                                      block.settings.stars
                                      | image_url: width: 120
                                      | image_tag: class: 'w-full max-w-[120px]', sizes: '120px'
                                    }}
                                  {% endif %}

                                  {% if block.settings.description != blank %}
                                    <div class="text-[16px] md:text-[17px] font-normal !leading-[1.5] text-[#252726] text-center md:text-left">
                                      {{ block.settings.description }}
                                    </div>
                                  {% endif %}

                                  {% if block.settings.name != blank %}
                                    <div class="flex items-center justify-between gap-2 w-full">
                                      {% if block.settings.name != blank %}
                                        <div class="flex items-center gap-3">
                                          {% if block.settings.avatar != blank %}
                                            {{
                                              block.settings.avatar
                                              | image_url: width: 24
                                              | image_tag: class: 'w-full max-w-[24px] rounded-[30px]', sizes: '24px'
                                            }}
                                          {% endif %}
                                          <div class="text-[14px] md:text-[16px] font-bold !leading-tight text-[#252726]">
                                            {{ block.settings.name }}
                                          </div>
                                        </div>
                                      {% endif %}

                                      <div class="flex items-center gap-2">
                                        {% if block.settings.icon != blank %}
                                          {{
                                            block.settings.icon
                                            | image_url: width: 14
                                            | image_tag: class: 'w-full max-w-[14px]', sizes: '14px'
                                          }}
                                        {% endif %}
                                        {% if block.settings.verified != blank %}
                                          <div class="text-[15px] md:text-[15px] font-[300] !leading-tight text-[#252726]">
                                            {{ block.settings.verified }}
                                          </div>
                                        {% endif %}
                                      </div>
                                    </div>
                                  {% endif %}
                                </div>
                              </div>
                            {% endif %}
                        {% endcase %}
                      {% endfor %}
                    </div>
                  {% endif %}

                  {% if block.settings.animation_parent %}
                    {% assign parent_id = block.settings.id %}
                    <div class="bg-[#1E3A16] py-4 overflow-hidden px-5 md:px-10">
                      <div class="cr-unique-s2-animation flex items-center">
                        {% for i in (1..10) %}
                          <div class="nl-animation__item flex items-center justify-between max-w-[1200px] min-w-[1200px] px-5">
                            {% for block in section.blocks %}
                              {% if block.type == 's2_animation' %}
                                {% if parent_id == block.settings.id %}
                                  <div class="flex items-center justify-center gap-2">
                                    {% if block.settings.icon != blank %}
                                      {{
                                        block.settings.icon
                                        | image_url: width: 16
                                        | image_tag: class: 'w-full max-w-[16px]', sizes: '16px'
                                      }}
                                    {% endif %}
                                    {% if block.settings.description != blank %}
                                      <div
                                        class="text-[17px] md:text-[18px] font-bold !leading-[1] text-[#fff]"
                                      >
                                        {{ block.settings.description }}
                                      </div>
                                    {% endif %}
                                  </div>
                                {% endif %}
                              {% endif %}
                            {% endfor %}
                          </div>
                        {% endfor %}
                      </div>
                    </div>

                    <style>
                      .cr-unique-s2-animation {
                        animation: CRUniqueS2Animation 50s linear infinite;
                        width: 6000px;
                      }
                      @keyframes CRUniqueS2Animation {
                        0% {
                          transform: translateX(0);
                        }
                        100% {
                          transform: translateX(-3000px);
                        }
                      }
                    </style>
                  {% endif %}

                  {% if block.settings.card_parent %}
                    {% assign parent_id = block.settings.id %}
                    <div id="{{ parent_id }}" class="w-full px-5 md:px-10 mb-[40px] md:mb-[80px]">
                      <div class="splide cr-s2-carousel">
                        <div class="splide__track">
                          <div class="splide__list">
                            {% for block in section.blocks %}
                              {% case block.type %}
                                {% when 's2_card' %}
                                  {% if parent_id == block.settings.id %}
                                    <div class="splide__slide h-auto">
                                      <div
                                        data-id="{{ block.id }}"
                                        data-p-id="{{ parent_id }}"
                                        class="flex flex-col items-center gap-10 text-center md:text-left md:grid md:grid-cols-3 bg-white rounded-[20px] p-5 md:p-8 h-full"
                                      >
                                        {% if block.settings.image != blank %}
                                          {{
                                            block.settings.image
                                            | image_url: width: 300
                                            | image_tag:
                                              class: 'w-full max-w-[70%] md:max-w-[80%] mx-auto object-contain'
                                          }}
                                        {% endif %}
                                        <div class="md:col-span-2 flex flex-col max-md:items-center max-md:text-center gap-4">
                                          {% if block.settings.heading != blank %}
                                            <h2
                                              class="text-[20px] md:text-[20px] font-bold !leading-tight text-[#252726] md:text-[#fff] text-center md:bg-[#084C25] md:px-1 md:py-[2px] w-fit"
                                            >
                                              {{ block.settings.heading }}
                                            </h2>
                                          {% endif %}
                                          {% if block.settings.description != blank %}
                                            <div
                                              class="mt-3 text-[16px] md:text-[16px] font-normal !leading-tight text-[#252726] [&>ul]:list-disc [&>ul]:text-left [&>ul]:mx-auto [&>ul]:w-fit [&>ul>li]:w-fit [&>ul]:max-md:bg-white [&>ul]:max-md:rounded-[10px] [&>ul]:max-md:p-3 [&>ul]:!pl-7"
                                            >
                                              {{ block.settings.description }}
                                            </div>
                                          {% endif %}
                                        </div>
                                      </div>
                                    </div>
                                  {% endif %}
                              {% endcase %}
                            {% endfor %}
                          </div>
                        </div>
                      </div>
                    </div>
                    <script>
                      document.addEventListener('DOMContentLoaded', function () {
                        var splide = new Splide('.section-{{ section.id }}-padding #{{ parent_id }} .cr-s2-carousel', {
                          perPage: 1,
                          perMove: 1,
                          rewind: true,
                          gap: '20px',
                          trimSpace: false,
                          pagination: false,
                          arrows: true,
                          padding: { left: '0px', right: '0' },
                          breakpoints: {
                            767: {
                              perPage: 1,
                              perMove: 1,
                              arrows: false,
                              pagination: true,
                              gap: '12px',
                              padding: { right: '0', left: '0' },
                            },
                          },
                        });
                        splide.mount();
                      });
                    </script>
                  {% endif %}
                </div>
            {% endcase %}
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  document.querySelectorAll('.car-unique-s1-tab').forEach((tab) => {
    tab.addEventListener('click', function () {
      const pId = this.getAttribute('data-p-id');
      const targetId = this.getAttribute('data-id');

      document
        .querySelectorAll(`.car-unique-s1-tab-img[data-p-id="${pId}"]`)
        .forEach((img) => img.classList.remove('active'));

      const targetImg = document.querySelector(`.car-unique-s1-tab-img[data-p-id="${pId}"][data-id="${targetId}"]`);
      if (targetImg) {
        targetImg.classList.add('active');
      }
    });
  });
</script>

<script>
  document.querySelectorAll('.car-unique-s1-card').forEach((card) => {
    card.addEventListener('click', function () {
      const pId = this.getAttribute('data-p-id');
      const targetId = this.getAttribute('data-id');

      // Remove 'active' from all cards in the same group
      document.querySelectorAll(`.car-unique-s1-card[data-p-id="${pId}"]`).forEach((c) => c.classList.remove('active'));

      // Add 'active' to the clicked card
      this.classList.add('active');

      // Remove 'active' from all images in the same group
      document
        .querySelectorAll(`.car-unique-s1-card-one[data-p-id="${pId}"]`)
        .forEach((img) => img.classList.remove('active'));

      // Add 'active' to the matching image
      const targetImg = document.querySelector(`.car-unique-s1-card-one[data-p-id="${pId}"][data-id="${targetId}"]`);
      if (targetImg) {
        targetImg.classList.add('active');
      }
    });
  });
</script>

<style>
  .section-{{ section.id }}-padding .cr-s2-carousel .splide__arrow--prev {
    bottom: -120px;
    top: auto;
    width: 54px;
    height: 54px;
    background: transparent;
    border: 1px solid {{ section.settings.arrow_color }};
    right: auto;
    left: calc(50% - 70px);
  }
  .section-{{ section.id }}-padding .cr-s2-carousel .splide__arrow--next {
    bottom: -120px;
    top: auto;
    width: 54px;
    height: 54px;
    background: transparent;
    border: 1px solid {{ section.settings.arrow_color }};
    right: calc(50% - 70px);
  }
  .section-{{ section.id }}-padding .cr-s2-carousel .splide__arrow svg {
    fill: {{ section.settings.arrow_color }};
  }
  .section-{{ section.id }}-padding .cr-s2-carousel .splide__pagination__page {
    width: 10px;
    height: 10px;
    background: transparent;
    border: 1px solid {{ section.settings.arrow_color }};
  }
  .section-{{ section.id }}-padding .cr-s2-carousel .splide__pagination__page.is-active {
    width: 40px;
    border-radius: 20px;
    background: {{ section.settings.arrow_color }};
    opacity: 1;
    transform: unset;
  }
  .section-{{ section.id }}-padding .cr-s2-carousel .splide__pagination {
    bottom: -32px;
    position: relative;
  }
</style>

{% schema %}
{
  "name": "CR Unique",
  "settings": [
    {
      "type": "image_picker",
      "id": "bg",
      "label": "Background"
    },
    {
      "type": "color",
      "id": "arrow_color",
      "label": "Arrow Color",
      "default": "#084C25"
    },
    {
      "type": "range",
      "id": "max_width",
      "min": 600,
      "max": 1800,
      "step": 20,
      "unit": "px",
      "label": "Max Width",
      "default": 1200
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding Top",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_top_m",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding Top (Mobile)",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom_m",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding Bottom (Mobile)",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "s1_contents",
      "name": "S1 Contents",
      "settings": [
        {
          "type": "text",
          "id": "id",
          "label": "ID"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading"
        },
        {
          "type": "color",
          "id": "heading_color",
          "label": "Heading Color",
          "default": "#00754A"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description"
        },
        {
          "type": "color",
          "id": "description_color",
          "label": "Description Color",
          "default": "#252726"
        },
        {
          "type": "richtext",
          "id": "img_label",
          "label": "Image Label"
        },
        {
          "type": "color",
          "id": "img_label_color",
          "label": "Image Label Color",
          "default": "#252726"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "checkbox",
          "id": "tab_parent",
          "label": "Tab Parent",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "card_parent",
          "label": "Card Parent",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "hide_mobile",
          "label": "Hide Mobile",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "hide_desktop",
          "label": "Hide Desktop",
          "default": false
        }
      ]
    },
    {
      "type": "s1_tab",
      "name": "S1 Tab",
      "settings": [
        {
          "type": "text",
          "id": "id",
          "label": "ID"
        },
        {
          "type": "richtext",
          "id": "img_label",
          "label": "Image Label"
        },
        {
          "type": "color",
          "id": "img_label_color",
          "label": "Image Label Color",
          "default": "#fff"
        },
        {
          "type": "color",
          "id": "img_label_bgcolor",
          "label": "Image Label BG Color",
          "default": "#084C25"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "checkbox",
          "id": "active",
          "label": "Active",
          "default": false
        }
      ]
    },
    {
      "type": "s1_card",
      "name": "S1 Card",
      "settings": [
        {
          "type": "text",
          "id": "id",
          "label": "ID"
        },
        {
          "type": "text",
          "id": "tab",
          "label": "Tab"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "heading1",
          "label": "Heading 1"
        },
        {
          "type": "text",
          "id": "heading2",
          "label": "Heading 2"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description"
        },
        {
          "type": "checkbox",
          "id": "active",
          "label": "Active",
          "default": false
        }
      ]
    },
    {
      "type": "s2_contents",
      "name": "S2 Contents",
      "settings": [
        {
          "type": "text",
          "id": "id",
          "label": "ID"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading"
        },
        {
          "type": "color",
          "id": "heading_color",
          "label": "Heading Color",
          "default": "#00754A"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description"
        },
        {
          "type": "color",
          "id": "description_color",
          "label": "Description Color",
          "default": "#252726"
        },
        {
          "type": "richtext",
          "id": "img_label",
          "label": "Image Label"
        },
        {
          "type": "color",
          "id": "img_label_color",
          "label": "Image Label Color",
          "default": "#252726"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "checkbox",
          "id": "review_parent",
          "label": "Review Parent",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "animation_parent",
          "label": "Animation Parent",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "card_parent",
          "label": "Card Parent",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "hide_mobile",
          "label": "Hide Mobile",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "hide_desktop",
          "label": "Hide Desktop",
          "default": false
        }
      ]
    },
    {
      "type": "s2_review",
      "name": "S2 Review",
      "settings": [
        {
          "type": "text",
          "id": "id",
          "label": "ID"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "image_picker",
          "id": "stars",
          "label": "Stars"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name"
        },
        {
          "type": "image_picker",
          "id": "avatar",
          "label": "Avatar"
        },
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Icon"
        },
        {
          "type": "text",
          "id": "verified",
          "label": "Verified"
        }
      ]
    },
    {
      "type": "s2_animation",
      "name": "S2 Animation",
      "settings": [
        {
          "type": "text",
          "id": "id",
          "label": "ID"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description"
        },
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Icon"
        }
      ]
    },
    {
      "type": "s2_card",
      "name": "S2 Card",
      "settings": [
        {
          "type": "text",
          "id": "id",
          "label": "ID"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "CR Unique"
    }
  ]
}
{% endschema %}

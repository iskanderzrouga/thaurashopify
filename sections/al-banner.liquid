{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top_m }}px;
    padding-bottom: {{ section.settings.padding_bottom_m }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="section-{{ section.id }}-padding px-5 relative" style="background-color: {{ section.settings.bgcolor }}">
  {% if section.settings.bg_image != blank %}
    {{
      section.settings.bg_image
      | image_url: width: 1900
      | image_tag: class: 'w-full h-full absolute top-0 left-0 object-cover', sizes: '1900px'
    }}
  {% endif %}

  {% if section.settings.video_url != blank %}
    <video
      src="{{ section.settings.video_url }}"
      type="video/mp4"
      class="w-full h-full absolute top-0 left-0 object-cover {% if section.settings.video_url_m != blank %} hidden md:block {% endif %}"
      loop
      muted
      playsinline
      autoplay
      disablepictureinpicture
      controlslist="nodownload"
      height="auto"
      style="width:100%;"
    ></video>
  {% endif %}

  {% if section.settings.video_url_m != blank %}
    <video
      src="{{ section.settings.video_url_m }}"
      type="video/mp4"
      class="w-full h-full absolute top-0 left-0 object-cover block md:hidden"
      loop
      muted
      playsinline
      autoplay
      disablepictureinpicture
      controlslist="nodownload"
      height="auto"
      style="width:100%;"
    ></video>
  {% endif %}

  <div
    class="mx-auto w-full relative min-h-[450px] md:min-h-[650px] flex items-center"
    style="max-width: {{ section.settings.max_width }}px;"
  >
    <div class="flex flex-col h-full justify-center gap-6 md:gap-12">
      {% if section.settings.heading != blank %}
        <h2 class="text-[26px] md:text-[40px] font-bold !leading-tight text-[#272727]">
          {{ section.settings.heading }}
        </h2>
      {% endif %}

      {% if section.settings.description != blank %}
        <div class="text-[17px] md:text-[20px] font-normal !leading-tight text-[#272727]">
          {{ section.settings.description }}
        </div>
      {% endif %}

      {% if section.blocks.size > 0 %}
        <div class="flex flex-col items-center justify-center md:items-start gap-6 md:max-w-[660px] text-center md:text-left">
          {% for block in section.blocks %}
            {% case block.type %}
              {% when 'heading' %}
                {% if block.settings.heading != blank %}
                  <div
                    class="text-[36px] md:text-[62px] font-bold !leading-tight text-[#272727]"
                    style="color: {{section.settings.color_primary}}"
                  >
                    {{ block.settings.heading }}
                  </div>
                {% endif %}

              {% when 'description' %}
                {% if block.settings.description != blank %}
                  <div
                    class="text-[17px] md:text-[20px] font-normal !leading-tight text-[#272727]"
                    style="color: {{section.settings.color_primary}}"
                  >
                    {{ block.settings.description }}
                  </div>
                {% endif %}

              {% when 'button' %}
                {% if block.settings.button_label != blank %}
                  <div class="w-full md:w-fit mx-auto md:mx-0 mt-6">
                    <a
                      href="{{ block.settings.button_url }}"
                      class="new-button--secondary"
                    >
                      {{ block.settings.button_label }}
                      {%- render 'icon-arrow-right' -%}
                    </a>
                  </div>
                {% endif %}
            {% endcase %}
          {% endfor %}
        </div>
      {% endif %}

      {% if section.settings.button_label != blank %}
        <div class="w-full md:w-fit mx-auto">
          <a
            href="{{ section.settings.button_url }}"
            class="new-button--primary"
          >
            {{ section.settings.button_label }}
            {%- render 'icon-arrow-right' -%}
          </a>
        </div>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "AL Banner",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button URL"
    },
    {
      "type": "color",
      "id": "bgcolor",
      "label": "background Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "color_primary",
      "label": "Primary Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "color_secondary",
      "label": "Secondary Color",
      "default": "#00176B"
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "BG Image"
    },
    {
      "type": "text",
      "id": "video_url",
      "label": "Video URL"
    },
    {
      "type": "text",
      "id": "video_url_m",
      "label": "Video URL (Mobile)"
    },
    {
      "type": "range",
      "id": "max_width",
      "min": 600,
      "max": 1800,
      "step": 20,
      "unit": "px",
      "label": "Max Width",
      "default": 1200
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding Top",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_top_m",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding Top (Mobile)",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom_m",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding Bottom (Mobile)",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "heading",
      "name": "Heading",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading"
        }
      ]
    },
    {
      "type": "description",
      "name": "Description",
      "settings": [
        {
          "type": "richtext",
          "id": "description",
          "label": "Description"
        }
      ]
    },
    {
      "type": "button",
      "name": "Button",
      "settings": [
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label"
        },
        {
          "type": "url",
          "id": "button_url",
          "label": "Button URL"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "AL Banner"
    }
  ]
}
{% endschema %}
